'use strict';

describe('Service: Login', function () {

  beforeEach(module('angularSeed'));

  var Login;

  beforeEach(inject(function (_Login_) {
    Login = _Login_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
