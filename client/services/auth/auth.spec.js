'use strict';

describe('Service: Auth', function () {

  beforeEach(module('angularSeed'));

  var Auth;

  beforeEach(inject(function (_Auth_) {
    Auth = _Auth_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
