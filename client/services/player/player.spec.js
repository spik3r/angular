'use strict';

describe('Factory: Player', function () {

  beforeEach(module('angularSeed'));

  var Player;

  beforeEach(inject(function (_Player_) {
    Player = _Player_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
