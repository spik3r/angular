'use strict';

describe('Service: PhotoEdit', function () {

  beforeEach(module('angularSeed'));

  var PhotoEdit;

  beforeEach(inject(function (_PhotoEdit_) {
    PhotoEdit = _PhotoEdit_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
