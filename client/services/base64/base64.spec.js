'use strict';

describe('Factory: Base64', function () {

  beforeEach(module('eiFrontend'));

  var Base64;

  beforeEach(inject(function (_Base64_) {
    Base64 = _Base64_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
