'use strict';

describe('Service: Log', function () {

  beforeEach(module('eiFrontend'));

  var Log;

  beforeEach(inject(function (_Log_) {
    Log = _Log_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
